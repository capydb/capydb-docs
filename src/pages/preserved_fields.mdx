# preserved fields

All fields that start with `_` (under bar) are preserved meaning you are not allowed assign or modify values in those fields. Any fields that do not start with `_` can be used as you like without limitation.
